[20250621] 작업계획
    # 상용서버에 현재 개발된 소스를 docker를 통해 배포테스트를 하고 싶다.  
        1. 모든 배포는 GitHub 에 push 된 소스를 기반으로 배포되게 하고 싶다. 
            # 상용서버는 현재 리눅스 서버 (우분투) 를 사용중이다. 
        2. 웹서버를 담당하는 nginx 는 이미 개발서버에 docker 가 아닌 물리적으로 설치 되어 있다. 
        3. @/backend 와 @/frontend 는 하나의 docker container 로 구성되어도 무방하다. 
            # 이경우 백앤드만 반영할때는 어떻게 되는건지 알려주세요.
        
        4. 백앤드와 프론트앤드는 nginx 상에 리버스프록시 형태로 구성설정 하고 싶으니 관련 설정가이드를 해주세요.  
        5. 현재 .env 등이 개발용으로 설정되어 있으니, 이분은 어떻게 분리 하면 좋을지 알려주면 좋겠습니다. 
            # 로그생성 폴더, 로깅레벨등 

        6. 특정일자 주기로 자동배포가 되면 좋겠다.
        7. DB서버는 이미 설치되어 있어 별도 docker 등으로 관리 안해도 된다. 

    #기타 
        1. 로컬에서 특정 python 등의 배포 프로그램을 만들어서 내  PC에서도 배포가 가능한지 알고 싶다. 
            # 젠킨스 등의 별도 툴은 없는 상태 이다. 
            # 예를 들어 원격 도커를 관리하거나 할수 있는 프로그램이 있는지? 

    # 제약사항 
        1. 기술검토를 고려하고 있는 상황이라, 우선 소스는 만들지 말고 가장효과적이고 심플한 전략으로 설명만 해주세요. 

    